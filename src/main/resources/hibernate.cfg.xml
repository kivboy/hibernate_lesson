<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection properties -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/sportclub</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">admin</property>

        <!-- PostgreSQL dialect -->
        <!-- <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property> -->

        <!-- Optional: Show SQL queries in console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Optional: Schema auto-update (create, update, validate, create-drop) -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping files or annotated classes -->
        <!-- Example for annotated class: -->
        <!-- <mapping class="com.example.YourEntityClass"/> -->

    </session-factory>
</hibernate-configuration>